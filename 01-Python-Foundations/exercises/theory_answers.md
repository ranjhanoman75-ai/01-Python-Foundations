1:What is python?
ans: Python is high level , programming language that is easy to read , easy to write , powerful, begginer-friendly. Python uses simple english language syntax, making it easier than other programming language.
2: Why is python is popular in AI and Agentic AI?
ans: It is very popular in Ai because it has huge ecosystem , libraries and tools , easy syntax, Massive ai libraries like NumPy, Pandas,Matplotlib, Scikit-learn, and so more. There are lot of LLM frameworks Like Langchain ,Langgraph,Crew Ai, Autogen.It has large community for support . It can run on multiple plateforms like window , Linux, and macOS.
3:What is an interpreter?
ans: An interpreter is software that reads and excutes your code one line at a time.Flow is like this , first python code, interpreter, machine code ,and output.
4: Difference between Compiler and interpreter.
ans: COMPILER: Translates the entire program before running, create an executable file,faster execution after compilation , reports many errors after compiling. e.g: C,C++
    INTERPRETER: Translates the code one line at a time, no separate executable file, slower execution, stops at first error . e.g: Python, Javascript, Python uses an interpreter 
5: What is variable?
ans: Variable is basically a named location in memory that stores the data . e.g: name stores "Noman".
6: Difference between = and == .
ans: (=): It is an assignment operator that assigns a value like age= 21 , it means that 21 is stored in age varible . (==): It is comparison operator that check the value or any other thing to compare each other like 4==4, result is true because they are both equal =. 
7: What are keywords?
ans: A keyword is reserved word in python with a predefined meaning . You cannot use keywords as variable names. like If ,else, while ,def, print, class, import etc.
